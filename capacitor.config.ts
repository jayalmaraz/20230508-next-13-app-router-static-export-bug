import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: '20230508t2038-my-next-app',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
